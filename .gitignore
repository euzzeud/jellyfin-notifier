# Secrets & runtime config
.env

# Python
venv/
__pycache__/
*.pyc
*.pyo

# Runtime data (state persisted by the service, not source)
seen_items.json
settings.json
pending_items.json
upcoming.json
mail_history.json

# Template backups generated by the admin editor
jellyfin_notifier/templates/backups/

# User-uploaded posters for manually-added "upcoming" titles (deployment
# content, not source)
jellyfin_notifier/uploads/

# Misc
*.log
.DS_Store
